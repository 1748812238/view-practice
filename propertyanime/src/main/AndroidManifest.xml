<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wondertwo.propertyanime">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BallsFallActivity" />
        <activity android:name=".XBallsFallActivity" />
        <activity android:name=".LinearProgress" />
        <activity android:name=".CircleProgress" />
        <activity android:name=".BuleToRed" />
        <activity android:name=".CustomPointActivity" />
        <activity android:name=".PositionDeAcActivity" />
    </application>

</manifest>
